# as const とリテラル型

このレッスンで学ぶこと

- `as const` で値を「読み取り専用のリテラル」に固定する
- 配列の要素を `"low"` のような「その文字列そのもの」の型にする
- 固定した配列から、リテラルのユニオン型を取り出す

<Callout type="info">
決まった選択肢(レベル・状態・色など)を、型として安全に扱えるようになります。
</Callout>
