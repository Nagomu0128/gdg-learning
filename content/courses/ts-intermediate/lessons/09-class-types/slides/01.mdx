# クラスと型

このレッスンで学ぶこと

- `implements` で「クラスが満たすべき形(interface)」を宣言する
- `private` / `public` でプロパティやメソッドの公開範囲を決める
- 型を付けたクラスを、関数から使う

<Callout type="info">
`private` にした値は、クラスの外から直接さわれなくなります。中身を守る大切なしくみです。
</Callout>
