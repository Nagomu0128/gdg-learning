# クラスと型

このレッスンで学ぶこと

- `implements` で「クラスが満たすべき形(interface)」を宣言する
- `private` / `public` でプロパティやメソッドの公開範囲を決める
- 型を付けたクラスを、関数から使う

<Callout type="info">
`private` は「クラスの外から直接は使わない」という型のうえの約束で、型チェックが守ってくれます。ただしこの演習環境では実行時に型が消えるため、実行だけを見ると外からでも代入できてしまいます。設計上の約束ごととして意識しましょう。
</Callout>
