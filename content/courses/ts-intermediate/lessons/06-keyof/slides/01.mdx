# keyof とインデックスアクセス

このレッスンで学ぶこと

- `keyof T` … 型 `T` のプロパティ名すべてを表す型
- `T[K]` … キー `K` に対応する値の型(インデックスアクセス型)
- この2つで「存在するキーの値だけ」を安全に取り出す

<Callout type="info">
存在しないキーを渡すと型エラーになるので、打ちまちがいを防げます。
</Callout>
