# やってみよう

演習では、ページの骨組みを用意してあります。コメント(`<!-- -->`)の場所に、次の2つを書き足しましょう。

1. `<head>` の中に、`<title>` タグで「はじめてのページ」という題名を書く
2. `<body>` の中に、`<h1>` タグで好きな見出しを書く

```html
<title>はじめてのページ</title>
```

<Callout type="info">
  タグの記号は半角で入力します。書けたら「できた!」ボタンで提出しましょう。
</Callout>
