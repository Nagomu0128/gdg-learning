# やってみよう

演習では、ブログ風のページを4つの区切りで組み立てます。`<header>` はすでに書いてあります。

1. `<nav>` を書き、中に `<a href="#">` のリンクを2つ入れる
2. `<main>` を書き、中に `<p>` で記事の文章を書く
3. `<footer>` を書き、中に `<p>` で「© わたしのブログ」と書く

<Callout type="info">

それぞれのタグの中身は1〜2行でかまいません。まずは4つの区切りをそろえることが目標です。

</Callout>
