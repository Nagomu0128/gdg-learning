# ページに意味のある区切りを

このレッスンでは、ページ全体を**意味のあるまとまり**に区切るタグを学びます。

- `<header>` — ページの上部(タイトルやロゴ)
- `<nav>` — メニュー(ページを案内するリンク集)
- `<main>` — ページの本文(いちばん伝えたい内容)
- `<footer>` — ページの下部(コピーライトなど)

こうしたタグは「セマンティック(意味のある)要素」と呼ばれます。
