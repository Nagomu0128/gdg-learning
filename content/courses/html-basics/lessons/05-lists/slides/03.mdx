# 番号つきリストはol

順番に意味があるリストは `<ol>` を使います。書き方は `<ul>` と同じで、項目は `<li>` で囲みます。

```html
<ol>
  <li>顔をあらう</li>
  <li>あさごはんを食べる</li>
</ol>
```

- 項目の先頭に 1. 2. 3. という番号が自動でつきます
- 手順やランキングなど「順番があるもの」は `<ol>`、そうでないものは `<ul>` と使い分けます

<Callout type="info">
  ol は ordered list(順序のあるリスト)の略です。
</Callout>
