# やってみよう

2つのリストを完成させましょう。

1. 「すきなくだもの」の `<ul>` に `<li>` を2つ書き足して、項目を合計3つにする
2. 「あさやること」の下に `<ol>` を作り、`<li>` を2つならべる

```html
<ol>
  <li>1番目にやること</li>
  <li>2番目にやること</li>
</ol>
```

<Callout type="info">
  li の数もチェックされます。ul の中は3個、ol の中は2個ちょうどにしましょう。書けたら「できた!」ボタンで提出です。
</Callout>
