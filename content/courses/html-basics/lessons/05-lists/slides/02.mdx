# 箇条書きはulとli

箇条書きは、全体を `<ul>` で囲み、項目を1つずつ `<li>` で囲みます。

```html
<ul>
  <li>りんご</li>
  <li>バナナ</li>
  <li>みかん</li>
</ul>
```

- `<li>` は必ず `<ul>` の中に入れます
- 項目の先頭には黒い点が自動でつきます

<Callout type="info">
  ul は unordered list(順序のないリスト)、li は list item(リストの項目)の略です。
</Callout>
