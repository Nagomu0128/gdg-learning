# 完成イメージ

今回作るページの構成です。`<body>` の中に、上から順に書いていきます。

1. `<h1>` — 名前の見出し(例: タロウのプロフィール)
2. `<img>` — アイコン画像。`src` は `icon.svg`、`alt` も忘れずに
3. `<ul>` と `<li>` — すきなものを**3つ**ならべる
4. `<a>` — すきなサイトへのリンク。`href` にURLを書く

見出しの文字や、すきなものの中身は、自分の好きな内容でかまいません。
