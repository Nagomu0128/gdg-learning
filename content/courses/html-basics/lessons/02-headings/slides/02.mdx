# 見出しはh1からh6まで

見出しのタグは `<h1>` から `<h6>` まで6段階あります。数字が小さいほど大きな見出しです。

```html
<h1>いちばん大きな見出し</h1>
<h2>その次に大きな見出し</h2>
<h3>もう一段小さな見出し</h3>
```

- `<h1>` はページ全体のタイトルとして、1ページに1つだけ使うのが基本です
- 章や節のような区切りには `<h2>` や `<h3>` を使います

<Callout type="info">
  h は heading(見出し)の頭文字です。
</Callout>
