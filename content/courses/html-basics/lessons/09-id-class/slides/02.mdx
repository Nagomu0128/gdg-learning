# id — 1つだけの名前

`id` 属性は、開始タグの中に書きます。

```html
<h1 id="main-title">おすすめの本</h1>
```

「このページでいちばん大事な見出し」のように、その要素だけを指す名前を付けたいときに使います。

<Callout type="warn">

同じ id の名前は、1つのページで1回しか使えません。id は「世界に1つだけの名札」です。

</Callout>
