# やってみよう

演習では、本のしょうかいページのタグに名前を付けます。タグ自体は書いてあるので、**属性だけ**を追加しましょう。

1. `<h1>` に `id="main-title"` を付ける
2. `<p>` に `class="intro"` を付ける
3. 3つの `<li>` すべてに `class="item"` を付ける

<Callout type="info">

属性は開始タグの中に、タグ名と半角スペースで区切って書きます。例: `<h1 id="main-title">`

</Callout>
