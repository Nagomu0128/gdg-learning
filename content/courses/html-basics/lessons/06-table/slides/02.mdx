# 表は「行」の集まり

HTMLの表は、`<table>` タグの中に**行**を上から順にならべて作ります。行を表すタグは `<tr>`(table row = 表の行)です。

```html
<table>
  <tr>
    (ここが1行目)
  </tr>
  <tr>
    (ここが2行目)
  </tr>
</table>
```

<Callout type="info">

表は「1行ずつ、上から下へ」組み立てていくイメージです。まず行を作り、そのあと行の中にセルを入れます。

</Callout>
