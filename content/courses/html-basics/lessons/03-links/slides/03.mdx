# 書き方の型

リンクの型はこれだけです。

```html
<a href="飛び先のURL">表示する文字</a>
```

- URL は必ず引用符 `"` で囲みます
- 表示する文字は、飛び先が分かる言葉にしましょう(「ここ」だけでは伝わりにくいです)

<Callout type="warn">
  引用符も半角の `"` で書きます。全角の＂では正しく動きません。
</Callout>
