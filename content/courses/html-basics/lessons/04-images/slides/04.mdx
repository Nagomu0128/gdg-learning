# やってみよう

うちのねこを紹介するページに、画像を1つのせましょう。

1. `<img>` タグを書く
2. `src` 属性に `cat.svg` を指定する
3. `alt` 属性に画像の説明(例: ねこのイラスト)を書く

```html
<img src="ファイル名" alt="画像の説明">
```

<Callout type="info">
  うまく書けると、プレビューにねこのイラストが表示されます。書けたら「できた!」ボタンで提出しましょう。
</Callout>
