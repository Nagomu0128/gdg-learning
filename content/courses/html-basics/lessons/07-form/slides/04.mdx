# やってみよう

演習では、おといあわせフォームを完成させます。

1. `<form>` の中に、`placeholder` 属性つきの `<input>` を書く
2. その下に、「送信」と表示される `<button>` を書く

<Callout type="info">

`<input>` に閉じタグはありません。`<button>` は中身の文字を書くのを忘れずに。

</Callout>
