# 入力欄をつくる — input

文字の入力欄は `<input>` タグで作ります。`<input>` には**閉じタグがありません**。

```html
<form>
  <input type="text">
</form>
```

`type="text"` は「1行の文字を入力する欄」という意味です。

<Callout type="info">

フォームの部品は、かならず `<form>` タグの中に入れます。

</Callout>
