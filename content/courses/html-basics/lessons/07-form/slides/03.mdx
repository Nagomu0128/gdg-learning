# placeholder とボタン

入力欄に `placeholder` 属性を付けると、入力前にうすい文字でヒントを表示できます。ボタンは `<button>` タグで作り、タグの中身がボタンの文字になります。

```html
<form>
  <input type="text" placeholder="お名前">
  <button>送信</button>
</form>
```

- `placeholder="お名前"` — 入力欄に「お名前」とうすく表示されます
- `<button>送信</button>` — 「送信」と書かれたボタンになります
