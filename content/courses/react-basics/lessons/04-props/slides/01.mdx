# props でデータを渡す

このレッスンで学ぶこと

- 親コンポーネントから子コンポーネントへデータを渡す方法
- 受け取る側の `props`(プロップス)
- 同じ部品を、渡す値を変えて使い回す考え方

<Callout type="info">
コンポーネントを「部品」だとすると、props は部品に取り付ける「設定」です。同じ部品でも設定を変えれば別の表示になります。
</Callout>
