# 複数要素をまとめる

このレッスンで学ぶこと

- コンポーネントが返せる要素は「1つ」だけというルール
- 複数の要素を1つにまとめるフラグメント `<> </>`
- 見出しと本文を並べて表示する

<Callout type="info">
「部品を1つ返す」のが React の約束です。でも実際の画面は複数の要素でできています。そのすき間を埋めるのがフラグメントです。
</Callout>
