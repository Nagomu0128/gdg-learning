# 入力を扱う

このレッスンで学ぶこと

- 入力欄(input)の文字を React で扱う方法
- 入力のたびに呼ばれる `onChange`
- state と入力欄をつなぐ「制御されたコンポーネント」

<Callout type="info">
入力欄の中身も state で持つのが React 流です。「入力 → state 更新 → 画面に反映」という流れを作ります。
</Callout>
