# やってみよう

`App` の中に、state・表示・3つのボタンを自分で書きます。

チェックされること

- `id="count"` に今の数が表示され、はじめは `0` である
- `id="plus"` を押すと1増える
- `id="minus"` を押すと1減る
- `id="reset"` を押すと `0` に戻る

<Callout type="info">
できたら「実行」で一通り押して動きを確かめ、「できた!」で判定しましょう。マイナスの数になっても大丈夫です。
</Callout>
