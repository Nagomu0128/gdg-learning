# 「あるときだけ」表示する

片方だけを出したいときは `&&` を使う書き方もあります。

```jsx
// エラーがあるときだけメッセージを出す
<div>
  {hasError && <p>エラーがあります</p>}
</div>
```

- `条件 && 要素` は「条件が true のときだけ要素を表示」します
- false のときは何も表示されません

<Callout type="info">
「A か B」を切り替えるなら三項演算子、「あるときだけ出す」なら `&&`、と使い分けます。今回の演習では三項演算子を使います。
</Callout>
