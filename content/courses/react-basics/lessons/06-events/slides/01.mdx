# クリックに反応する

このレッスンで学ぶこと

- ボタンのクリックに反応する `onClick`
- state を更新する関数 `setCount` の使い方
- 押すたびに画面の数が変わる仕組み

<Callout type="info">
前回の state に「変える手段」を足します。クリックで state を更新すると、React が自動で画面を作り直してくれます。
</Callout>
