# やってみよう

`commands.sh` に、解決までの 4 行を書きます。

- `git rebase main` の行で、赤いエラー(コンフリクト)が出ます。それで正解です
- 直して add したら、`git rebase --continue` で仕上げます

うまくいくと

```
rebase 完了
```

と表示され、履歴が 1 本の線にそろいます。

<Callout type="info">
途中でこんがらがったら `git rebase --abort` でリベース前に戻せます。何度でもやり直せるので、安心して試しましょう。
</Callout>
