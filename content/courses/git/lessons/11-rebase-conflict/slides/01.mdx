# rebase の衝突を解決

このレッスンで学ぶこと

- リベース中にもコンフリクト(衝突)は起きる
- 解決の手順はマージと似ているが、**最後の一手が違う**
- 直して `git add` したら、`git commit` ではなく `git rebase --continue` で続ける

<Callout type="info">
マージの衝突解決(git-09)を思い出しましょう。9 割は同じです。違うのは「続きの合図」だけです。
</Callout>
