# 変更を確認する

このレッスンで学ぶこと

- `git diff` — まだステージしていない変更(add 前)を見る
- `git diff --staged` — ステージ済みの変更(add 後・commit 前)を見る
- 「コミットする前に、何を変えたか自分の目で確かめる」習慣

<Callout type="info">
diff は「差分(ちがい)」という意味です。今のファイルと、記録済みの内容との「ちがい」を表示します。
</Callout>
