# diff の読み方

差分は、行の先頭の記号で「増えた・減った」を表します。

```
- こんにちは
+ おはよう
```

- `-` で始まる行 … 消えた(前の内容)
- `+` で始まる行 … 増えた(新しい内容)
- 記号なしの行 … 変わっていない行

<Callout type="warn">
commit する前に diff を見ると、「うっかり別の場所を変えていた」というミスに気づけます。記録の前のひと手間が、あとで効いてきます。
</Callout>
