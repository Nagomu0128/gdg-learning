# ブランチを作る

このレッスンで学ぶこと

- **ブランチ** = 作業の「枝分かれ」。本流(main)を止めずに、別の枝で試せる
- `git branch 名前` — ブランチを作る(今いる場所は変わらない)
- `git switch -c 名前` — ブランチを作って、そこへ移動する

<Callout type="info">
ブランチは「もしもの世界」を作る道具です。うまくいったら本流に取り込み、ダメなら捨てられます。本流を壊さずに実験できます。
</Callout>
