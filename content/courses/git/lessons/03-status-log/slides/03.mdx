# git log --oneline — 履歴を見る

`git log` は、これまでのコミットを新しい順に並べて見せてくれます。`--oneline` を付けると 1 コミット 1 行になります。

```
git log --oneline
```

表示はこんなイメージです(左が短いコミット番号、右がメッセージ)。

```
b1c2d3e 機能を追加
a0f9e8d 最初の実装
```

<Callout type="info">
コミットメッセージをていねいに書いておくと、この一覧が「開発の日記」になります。あとで自分を助けてくれます。
</Callout>
