# やってみよう

`commands.sh` に、状態と履歴を確認する 2 コマンドを書きます。

- `git status` — いまの状態(このリポジトリはきれいな状態のはずです)
- `git log --oneline` — 2 つのコミットが 1 行ずつ表示されます

どちらも状態を変えないので、何度実行しても大丈夫です。

<Callout type="info">
`git log`(--oneline なし)も試してみましょう。1 コミットにつき数行の、くわしい表示になります。
</Callout>
