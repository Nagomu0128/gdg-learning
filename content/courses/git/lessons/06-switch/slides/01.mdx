# ブランチを移動する

このレッスンで学ぶこと

- `git switch ブランチ名` で、すでにあるブランチへ移動する
- 「今いるブランチ」を切り替えると、作業ツリーの中身もそのブランチのものに入れ替わる
- `-c` を付けないのがポイント(付けると「新規作成」になってしまう)

<Callout type="info">
前のレッスンで作ったブランチを、今度は行き来します。ブランチは「作る」と「移動する」がセットで使えて、はじめて役に立ちます。
</Callout>
