# 変更を記録する

このレッスンで学ぶこと

- できあがったリポジトリに、新しい変更を「コミット」として積み重ねる
- `git add`(ステージ)と `git commit`(記録)の 2 段構え
- コミットは 1 つずつ増えていき、履歴になる

<Callout type="info">
このレッスンは、すでに README.md を 1 回コミットしたリポジトリから始まります。init は不要です。
</Callout>
