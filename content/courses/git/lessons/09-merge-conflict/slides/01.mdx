# コンフリクトを解決する

このレッスンで学ぶこと

- **コンフリクト(衝突)** = 同じ場所を両方のブランチが変えていて、Git が自動で決められない状態
- コンフリクトは「失敗」ではなく「あなたが決めてください」の合図
- 印を頼りに正しい内容へ書き直し、`add` と `commit` でマージを完了する流れ

<Callout type="info">
コンフリクトはチーム開発では日常です。こわがらず、落ち着いて「どちらを、どう残すか」を決めれば必ず解決できます。
</Callout>
