# フォーム部品図鑑

基礎編では `<input>`(1行の入力欄)と `<button>` を学びました。このレッスンでは、フォームの部品をさらに増やします。

- `<select>` と `<option>` — 選択肢から1つえらぶメニュー
- ラジオボタン — ならんだ選択肢から1つえらぶ
- `<textarea>` — 長い文章を書ける入力欄
- `<label>` の `for` 属性 — ラベルと入力欄をつなぐ

おやつアンケートのフォームを作りながら、使い方を覚えましょう。
