# やってみよう

演習では、気温レポートにグラフの図版をのせます。画像ファイル graph.svg は用意してあります。

1. `<figure>` タグを書く
2. 中に `<img src="graph.svg">` を書き、`alt` 属性で画像の内容を説明する
3. `<figcaption>` で「図1」ではじまるキャプションを書く(例: 図1: 今週の気温の変化)

<Callout type="info">
  キャプションは「図1」が入っていれば、続きの文章は自由です。
</Callout>
