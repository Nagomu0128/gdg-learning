# figureとfigcaptionの型

図版を `<figure>` で囲み、その中に `<img>` と `<figcaption>` を入れます。

```html
<figure>
  <img src="graph.svg" alt="気温の変化を表す折れ線グラフ">
  <figcaption>図1: 今週の気温の変化</figcaption>
</figure>
```

- `<figcaption>` は figure の中に1つだけ。先頭か最後に置きます
- 画像だけでなく、表やコード例のまとまりに使うこともできます
