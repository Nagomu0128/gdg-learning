# h2とh3で階層を作る

章(`<section>`)の見出しには `<h2>` を、章の中をさらに区切る小見出しには `<h3>` を使います。

```html
<section>
  <h2>観察の記録</h2>
  <p>成長のようすを記録していきます。</p>
  <h3>7月の記録</h3>
  <p>つるがのびて、花がさきはじめました。</p>
</section>
```

- 「h1 → h2 → h3」と、上から1段ずつ下げるのが基本の型です
- h2 の次にいきなり h4 を使うなど、階層をとばすのは避けましょう
