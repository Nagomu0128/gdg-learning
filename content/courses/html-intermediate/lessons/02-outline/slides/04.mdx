# やってみよう

演習では、観察日記のページを見出しで設計します。`<h1>` と1つ目の `<section>` はできあがっています。

1. 2つ目の `<section>` の中に `<h2>` で「観察の記録」という見出しを書く
2. 同じ section の中に `<h3>` で「7月の記録」という見出しを書く

<Callout type="info">
  h1 が1つ・h2 が2つ・h3 が1つ、という階層になれば完成です。
</Callout>
