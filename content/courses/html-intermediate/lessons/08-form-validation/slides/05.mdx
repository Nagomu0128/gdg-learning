# やってみよう

演習では、会員登録フォームに入力チェックを付けます。

1. ニックネームの input に `required` と `maxlength="8"` を付ける
2. メールアドレスの input の type を `email` に変えて、`required` も付ける
3. 郵便番号の input に `pattern="[0-9]{3}-[0-9]{4}"` を付ける

<Callout type="warn">

pattern の中の記号(`[` `]` `{` `}` `-`)はすべて半角です。1文字ずつていねいに書き写しましょう。

</Callout>
