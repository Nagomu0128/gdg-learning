# 入力チェックはブラウザにおまかせ

フォームには「必ず入力してほしい」「メールアドレスの形で入力してほしい」といった**ルール**がつきものです。

HTML では、input に属性を付けるだけで、ブラウザがルール違反をチェックして教えてくれます。

- `required` — 入力を必須にする
- `maxlength` — 文字数の上限を決める
- `type="email"` — メールアドレスの形かチェックする
- `pattern` — 自分で決めた形かチェックする

JavaScript を1行も書かなくても、ここまでできます。
