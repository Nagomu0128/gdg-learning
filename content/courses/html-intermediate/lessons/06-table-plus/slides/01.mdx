# 表の構造化

基礎編では、`<table>`・`<tr>`・`<th>`・`<td>` で表を作りました。このレッスンでは、表を**構造ごとに整理する**タグと属性を学びます。

- `<caption>` — 表のタイトル
- `<thead>` / `<tbody>` — 見出しの行・データの行のまとまり
- `scope` 属性 — 見出しの「向き」を伝える
- `colspan` 属性 — セルを横につなげる

見た目は同じでも、しっかり構造化された表は「機械や読み上げソフトにも伝わる表」になります。
