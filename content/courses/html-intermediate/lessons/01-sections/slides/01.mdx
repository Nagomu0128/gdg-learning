# ページを区画する

このレッスンでは、ページの中身を**意味のあるまとまり**に区切る3つのタグを学びます。

- `<section>` — テーマごとの「章」のようなまとまり
- `<article>` — それだけで1つの読みものとして成立するまとまり(記事)
- `<aside>` — 本筋から少し外れた補足やお知らせ

基礎編で学んだ `<header>` や `<main>` の仲間で、ページの構造を意味で伝えるセマンティック要素です。
