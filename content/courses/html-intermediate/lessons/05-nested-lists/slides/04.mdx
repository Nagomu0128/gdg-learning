# やってみよう

演習では、旅のじゅんびページを2つのリストで仕上げます。

1. 「おやつ」の `<li>` の中に `<ul>` を書き、`<li>` でおやつを2つならべる
2. 「ことばの説明」の見出しの下に `<dl>` を書き、`<dt>`(ことば)と `<dd>`(説明)のペアを2組ならべる

<Callout type="info">
  おやつやことばの中身は自由です。数(入れ子の li が2個、dt と dd が2個ずつ)がそろえば合格です。
</Callout>
