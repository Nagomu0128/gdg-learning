# やってみよう

`count` から合計金額を計算し、`#total` に表示しましょう。1個 100 円です。

- `const total = count * 100;` で派生値を計算します
- `<p id="total">{total}</p>` で表示します
- はじめは 100、「1個ふやす」を押すたびに 200・300 と増えれば成功です

<Callout type="info">
`total` を state にせず計算するだけで、`count` と常にピッタリ合うのがポイントです。
</Callout>
