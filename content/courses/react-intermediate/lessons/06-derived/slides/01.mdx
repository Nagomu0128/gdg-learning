# 計算した値を表示

このレッスンで学ぶこと

- 他の値から計算で出せる「派生値(derived value)」
- 派生値は state にせず、描画のたびに計算する
- state を増やしすぎないコツ

<Callout type="info">
「個数 × 単価 = 合計」のように、既にある state から計算できる値は、わざわざ別の state にしません。計算して表示するだけで十分です。
</Callout>
