# やってみよう

`remove` 関数の中で `filter` を使い、`target` と一致しない要素だけを残しましょう。

- `items.filter((item) => item !== target)` で残す要素を集めます
- 結果を `setItems` に渡します
- どれかの「削除」を押すと `li` が3つから2つに減れば成功です

<Callout type="info">
「追加はスプレッド、削除は filter」。配列 state の基本の2パターンです。
</Callout>
