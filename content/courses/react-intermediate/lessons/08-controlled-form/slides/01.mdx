# 複数フィールドのフォーム

このレッスンで学ぶこと

- 複数の入力を1つの state オブジェクトで管理する
- 入力の `name` 属性で「どの項目か」を見分ける
- 計算されたキー `[e.target.name]` で1つの関数にまとめる

<Callout type="info">
入力欄ごとに state を作ると、数が増えるほど大変になります。関連する入力はまとめて1つのオブジェクトにし、更新も1つの関数で処理しましょう。
</Callout>
