# やってみよう

`add` 関数の中で、末尾に `"バナナ"` を足した新しい配列を `setItems` に渡しましょう。

- `[...items, "バナナ"]` の形で新しい配列を作ります
- 「追加」を押すと `li` が2つから3つに増え、末尾が「バナナ」になれば成功です

<Callout type="info">
「広げて、足して、渡す」。配列の追加はこの3ステップで覚えましょう。
</Callout>
