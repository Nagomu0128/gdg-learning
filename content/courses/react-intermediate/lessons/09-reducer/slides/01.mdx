# useReducer で状態管理

このレッスンで学ぶこと

- `useState` とは別の状態管理フック `React.useReducer`
- 更新ルールを1か所(reducer)にまとめる考え方
- `dispatch` で「アクション」を送って状態を変える

<Callout type="info">
状態の更新パターンが増えてくると、`useState` の `setXxx` があちこちに散らばります。`useReducer` は「更新ルール」を reducer 関数にまとめ、見通しをよくします。
</Callout>
