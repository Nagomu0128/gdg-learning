# 関数を子に渡す

このレッスンで学ぶこと

- props で「関数」を子コンポーネントに渡す
- 子から親の state を更新してもらう仕組み
- `onXxx` という名前付けの慣習

<Callout type="info">
props はデータを渡すだけのものではありません。関数を渡せば、子で起きた出来事(クリックなど)を親に伝えて、親の state を更新できます。
</Callout>
