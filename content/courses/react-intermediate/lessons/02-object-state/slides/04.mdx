# やってみよう

`birthday` 関数の中で `setUser` を呼び、`age` だけ 1 増やしましょう。

- `{ ...user, age: user.age + 1 }` の形で新しいオブジェクトを作ります
- `name` は変えずにそのまま残します
- 「誕生日」を押すたびに年齢が 1 ずつ増え、名前は「タロウ」のまま変わらなければ成功です

<Callout type="info">
スプレッドで「コピーして一部だけ上書き」——これはオブジェクト state の更新の定番パターンです。
</Callout>
