# useRef で DOM 参照

このレッスンで学ぶこと

- 画面の DOM 要素を直接つかむための参照(ref)
- 参照を作るフック `React.useRef`
- ボタンで入力欄にフォーカスを当てる

<Callout type="info">
ふだんは state で画面を動かしますが、「この入力欄にカーソルを移す」のように DOM 要素そのものを操作したい場面もあります。そんなときが ref の出番です。
</Callout>
