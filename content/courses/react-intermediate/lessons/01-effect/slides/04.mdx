# やってみよう

`App` の中で `React.useEffect` を使い、マウント時に一度だけ `message` を `"読み込み完了"` に変えましょう。

- `React.useEffect(() => { ... }, [])` の形で書きます
- 中で `setMessage("読み込み完了")` を呼びます
- 最初は空だった `#status` が、少しして「読み込み完了」に変われば成功です

<Callout type="info">
副作用は「描いた後の追加作業」。まずは `[]` を付けて、一度きりの処理から慣れていきましょう。
</Callout>
