# useEffect で副作用

このレッスンで学ぶこと

- 描画の「ついで」に起こしたい処理 = 副作用(side effect)
- 副作用を書くためのフック `React.useEffect`
- マウント時(最初の表示)に一度だけ処理を動かす方法

<Callout type="info">
「データを読み込む」「メッセージを出す」「タイマーを仕掛ける」——こうした画面を描く本体とは別の処理を、React では副作用として `useEffect` にまとめます。
</Callout>
