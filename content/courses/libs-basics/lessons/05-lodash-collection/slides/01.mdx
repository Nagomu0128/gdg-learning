# lodashでデータ集計

このレッスンで学ぶこと

- オブジェクトの配列(データの一覧)を集計する
- `_.groupBy` で「種類ごと」にまとめる
- `_.sumBy` で「合計」を求める

<Callout type="info">
一覧データから「分類ごとの件数」や「合計金額」を出す処理は、集計の基本です。lodash を使うと、ループを書かずに読みやすくまとめられます。
</Callout>
