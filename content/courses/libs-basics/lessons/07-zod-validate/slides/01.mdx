# zodで入力チェック

このレッスンで学ぶこと

- `safeParse` で「正しいかどうか」を安全に受け取る
- `parse` との違い(エラーを投げるか、結果を返すか)
- 検証結果で表示を「OK / エラー」に切り替える

<Callout type="info">
ユーザーの入力は、空だったり形が違ったりします。エラーで処理が止まると困る場面では、`safeParse` で結果を受け取って自分で分岐するのが安全です。
</Callout>
