# やってみよう

`main.js` の `deadline` と `total` を、自分で書いてみましょう。

- `deadline`: `deadlineDate` を `"YYYY年MM月DD日"` に整形(dayjs の `format`)
- `total`: `tasks` の `points` の合計(lodash の `_.sumBy`)
- 「▶ 実行」で締め切り 2026年04月01日 と合計ポイント 10 が表示されれば成功です

<Callout type="info">
これでライブラリ入門は完了です。script タグで読み込み、dayjs・lodash・zod を使い、複数を組み合わせるところまで到達しました。おつかれさまでした。
</Callout>
