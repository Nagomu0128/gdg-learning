# zodでデータ検証

このレッスンで学ぶこと

- zod は「データの形」をチェックするライブラリ
- `z.object` でスキーマ(あるべき形)を定義する
- `parse` で、値がその形に合っているか検証する

<Callout type="info">
外部から届くデータ(フォーム入力や API の返事)は、期待どおりの形とは限りません。zod で「形の約束」を先に決めておくと、間違ったデータを早い段階で止められます。
</Callout>
