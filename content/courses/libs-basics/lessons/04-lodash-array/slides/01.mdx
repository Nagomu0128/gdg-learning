# lodashの配列ユーティリティ

このレッスンで学ぶこと

- lodash は「配列やオブジェクトの便利関数」を集めたライブラリ
- グローバル変数 `_`(アンダースコア)から関数を呼ぶ
- `_.chunk` で分割、`_.uniq` で重複除去

<Callout type="info">
配列を分割したり、重複を取り除いたりは自分でループを書いてもできますが、lodash なら 1 行です。名前が付いているので「何をしているか」も読みやすくなります。
</Callout>
