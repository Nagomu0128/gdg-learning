# dayjsで日付を計算

このレッスンで学ぶこと

- `add` で「◯日後・◯か月後」の日付を求める
- `diff` で「2 つの日付の差(日数)」を求める
- 固定の基準日から計算して、結果を表示する

<Callout type="info">
「締め切りの 3 日前」「登録から何日たったか」など、日付の足し算・引き算は実務でよく登場します。手計算だと月またぎでミスしがちな処理を dayjs が正確にやってくれます。
</Callout>
