# console.log() で表示する

`console.log()` は、( ) の中に書いたものを「コンソール」という画面に表示する命令です。

```js
console.log("Hello, World!");
```

書き方のポイントは3つです。

- 表示したい文字を `"`(ダブルクォート)で囲む
- 文字全体を `console.log(` と `)` ではさむ
- 行の最後にセミコロン `;` を書く

<Callout type="warn">記号はすべて半角で入力します。全角の（ ）や ； はエラーになるので注意しましょう。</Callout>
