# やってみよう

演習では、`id` が `greeting` の要素の文字を「こんにちは、JavaScript!」に書き換えます。

手順は 2 つです。

1. `document.getElementById("greeting")` で要素を取得する(初期コードに書いてあります)
2. 取得した要素の `textContent` に新しい文字を代入する

<Callout type="info">JS の実行は「実行」ボタンか Ctrl+Enter です。右の「見本」タブで完成形も確認できます。</Callout>

書けたら「できた!」ボタンで提出しましょう。
