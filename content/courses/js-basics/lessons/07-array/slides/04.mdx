# やってみよう

演習では、次の 2 つに挑戦します。

1. `console.log` と `length` を使って、配列 `fruits` の長さを出力する
2. 配列の最初の要素を返す関数 `firstItem` を完成させる

<Callout type="info">最初の要素の番号は 0 でしたね。`items[0]` で取り出せます。</Callout>

書けたら「できた!」ボタンで提出しましょう。
