# やってみよう

演習では、年齢によって入場できるかを判定する関数 `canEnter` を完成させます。

1. 関数の波かっこの中に if 文を書きます
2. `age` が 18 以上なら `true` を、そうでなければ `false` を返します
3. 「実行」で `true` と `false` が順に表示されたら、「できた!」で提出しましょう

<Callout type="info">「18 以上なら」という条件の書き方は、前のスライドを見直してみましょう。</Callout>
