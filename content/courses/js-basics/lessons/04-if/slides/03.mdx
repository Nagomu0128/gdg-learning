# 数をくらべる記号

if の条件には、数をくらべる式をよく書きます。

- `a >= b`: a は b 以上
- `a <= b`: a は b 以下
- `a > b`: a は b より大きい
- `a < b`: a は b より小さい
- `a === b`: a と b は等しい

たとえば「age が 18 以上」は `age >= 18` と書きます。

くらべた結果は、`true`(正しい)か `false`(正しくない)という特別な値になります。
