# if 文の形

「もし〜なら A、そうでなければ B」のように、条件によって処理を分けるのが if 文です。

```js
if (age >= 18) {
  console.log("入場できます");
} else {
  console.log("入場できません");
}
```

- `if (条件)` の条件が正しいとき、最初の波かっこの中が実行されます
- 条件が正しくないとき、`else` の波かっこの中が実行されます
