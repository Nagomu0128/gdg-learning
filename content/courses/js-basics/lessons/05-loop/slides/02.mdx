# for 文の形

同じ処理を何度もくり返すときは、for 文を使います。

```js
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

( ) の中は、セミコロンで区切られた3つの部分でできています。

- `let i = 1`: カウンタ変数 `i` を 1 から始める
- `i <= 5`: `i` が 5 以下のあいだ、くり返しを続ける
- `i++`: 1回くり返すたびに、`i` を 1 増やす

くり返したい処理は、波かっこの中に書きます。
