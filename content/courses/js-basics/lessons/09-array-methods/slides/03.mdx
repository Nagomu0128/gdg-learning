# アロー関数

`map` のカッコの中に書いた `(n) => n * 2` は「アロー関数」という、関数の短い書き方です。

```js
// この 2 つはほぼ同じ意味です
function double(n) {
  return n * 2;
}

const double2 = (n) => n * 2;
```

`=>` の左が引数、右が返す値です。`map` のように「その場で小さな処理を渡したいとき」によく使われます。
