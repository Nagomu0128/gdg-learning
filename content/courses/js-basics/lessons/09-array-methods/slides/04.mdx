# やってみよう

演習では、すべての要素を 2 倍にした新しい配列を返す関数 `doubleAll` をつくります。

```js
doubleAll([1, 2, 3]); // [2, 4, 6] を返す
```

<Callout type="info">`numbers.map` の結果を、そのまま `return` すれば完成です。</Callout>

書けたら「できた!」ボタンで提出しましょう。
