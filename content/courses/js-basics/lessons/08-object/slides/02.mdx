# オブジェクトをつくる

オブジェクトは `{ }` の中に「名前: 値」のペアをカンマで区切って書きます。

```js
const user = { name: "タロウ", age: 12 };
```

この「名前: 値」のペアを「プロパティ」と呼びます。`user` には `name` と `age` の 2 つのプロパティがあります。

<Callout type="info">配列は「番号」で、オブジェクトは「名前」で値を管理する、と覚えましょう。</Callout>
