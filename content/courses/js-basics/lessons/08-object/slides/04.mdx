# やってみよう

演習では、次の 2 つに挑戦します。

1. `console.log` で `user` の `name` を出力する
2. 受け取ったオブジェクトの `name` を返す関数 `getName` を完成させる

<Callout type="info">どちらも「オブジェクト名.プロパティ名」の形が書ければ完成します。</Callout>

書けたら「できた!」ボタンで提出しましょう。
