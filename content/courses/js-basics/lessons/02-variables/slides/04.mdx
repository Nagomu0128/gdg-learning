# やってみよう

演習では、変数に入れた名前を使って、自己紹介をコンソールに表示します。

`script.js` には、名前を変数 `myName` に入れる1行がすでに書いてあります。

1. その下に `console.log("ぼくの名前は" + myName);` を書き足します
2. 「実行」で `ぼくの名前はタロウ` と表示されるか確かめます
3. 表示できたら「できた!」で提出しましょう
