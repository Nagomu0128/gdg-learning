# 文字列と変数をつなげる

`+` を使うと、文字列と変数をつなげて1つの文字列にできます。

```js
const myName = "タロウ";
console.log("ぼくの名前は" + myName);
```

コンソールには `ぼくの名前はタロウ` と表示されます。

<Callout type="info">変数の名前はダブルクォートで囲みません。囲んでしまうと、変数ではなくただの文字列として表示されます。</Callout>
