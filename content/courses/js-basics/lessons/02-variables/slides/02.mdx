# 変数とは

変数は、値に名前をつけて入れておく「箱」です。`const` を使って、次のように書きます。

```js
const myName = "タロウ";
```

この1行は「`myName` という名前の箱に、`"タロウ"` という文字列を入れる」という意味です。

一度入れた値は、変数の名前を書くだけで何度でも取り出せます。

```js
console.log(myName);
```

コンソールには `タロウ` と表示されます。
