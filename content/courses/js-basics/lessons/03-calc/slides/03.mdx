# 変数どうしの計算

変数に入れた数も、そのまま計算に使えます。

```js
const price = 100;
const count = 3;

console.log(price * count);
```

`price` の中身は `100`、`count` の中身は `3` なので、`100 * 3` が計算されて `300` と表示されます。

値段や個数が変わっても、変数の中身を変えるだけで同じ式がそのまま使えます。これが変数を使う便利なところです。
