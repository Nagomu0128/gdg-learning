# やってみよう

演習では、2つの計算結果をコンソールに表示します。

1. 1つ目の `console.log()` の ( ) の中に、`price` と `count` をかけ算する式を書きます
2. 2つ目の `console.log()` の ( ) の中に、10 たす 5 の式を書きます
3. 「実行」で `300` と `15` が表示されたら、「できた!」で提出しましょう

<Callout type="info">かけ算の記号は半角の `*` です。× とは書かないので注意しましょう。</Callout>
