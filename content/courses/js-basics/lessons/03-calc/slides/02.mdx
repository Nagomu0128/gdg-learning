# 計算の記号

JavaScript では、次の記号(演算子)で計算します。

- たし算: `+`(例: `10 + 5`)
- ひき算: `-`(例: `10 - 5`)
- かけ算: `*`(例: `10 * 5`)
- わり算: `/`(例: `10 / 5`)

かけ算が × ではなく `*`、わり算が ÷ ではなく `/` なのがポイントです。

```js
console.log(10 + 5);
```

`console.log()` の ( ) の中に計算式を書くと、計算した結果が表示されます。この例では `15` と表示されます。
