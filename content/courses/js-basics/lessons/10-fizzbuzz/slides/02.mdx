# FizzBuzz のルール

数値 `n` を受け取って、次のルールで文字列を返す関数 `fizzbuzz` をつくります。

- 3 の倍数なら `"Fizz"` を返す
- 5 の倍数なら `"Buzz"` を返す
- 3 と 5 の両方の倍数なら `"FizzBuzz"` を返す
- どれでもなければ、数値を文字列にして返す(例: 7 なら `"7"`)

```js
fizzbuzz(3); // "Fizz"
fizzbuzz(5); // "Buzz"
fizzbuzz(15); // "FizzBuzz"
fizzbuzz(7); // "7"
```
