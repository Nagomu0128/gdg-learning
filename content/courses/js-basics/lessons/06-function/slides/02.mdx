# 関数の形

関数は、ひとまとまりの処理に名前をつけて、何度でも呼び出せるようにしたものです。

```js
function add(a, b) {
  return a + b;
}
```

- `function` に続けて、関数の名前(ここでは `add`)を書きます
- ( ) の中の `a` と `b` は「引数」です。呼び出すときに渡される値を受け取ります
- 波かっこの中に、実行したい処理を書きます

<Callout type="info">このコースでは、関数は必ず function を使った宣言で書きます。</Callout>
