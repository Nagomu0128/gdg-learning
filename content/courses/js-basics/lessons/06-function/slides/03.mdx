# return で答えを返す

`return` は、関数の計算結果を「答え」として呼び出し元に返す命令です。

```js
function add(a, b) {
  return a + b;
}

console.log(add(1, 2));
```

`add(1, 2)` と呼び出すと、`a` に 1、`b` に 2 が入り、`1 + 2` の答えの `3` が返ります。コンソールには `3` と表示されます。

<Callout type="info">return が実行されると、関数の処理はそこで終わります。</Callout>
