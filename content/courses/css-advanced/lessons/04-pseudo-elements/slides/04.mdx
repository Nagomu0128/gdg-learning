# やってみよう

おすすめスポットの各項目に、CSS だけで星印を付けます。`style.css` を編集しましょう。

1. `.item::before { }` のルールを書く(コロンは2つ)
2. その中に `content: "★";` を書いて、各項目の先頭に星を表示する
3. つづけて `color: orange;` を書いて、星だけオレンジ色にする

HTML(index.html)は編集できませんが、プレビューでは3つの項目すべてに星が付きます。「HTML を変えずに飾りが増える」のを確かめましょう。

書けたら「できた!」ボタンで判定します。
