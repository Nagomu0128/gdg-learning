# @keyframes アニメーション

このレッスンで学ぶこと

- 動きを定義する `@keyframes` の書き方
- 要素に動きを割り当てる `animation-name` / `animation-duration` / `animation-iteration-count`
- `transform: rotate()` による回転

JavaScript を1行も書かずに、くるくる回るローディングスピナーを作ります。
