# やってみよう

カフェのページで、おすすめカードがセールのバナーの下にかくれています。`style.css` を編集して、カードを手前に出しましょう。

1. `.card` のコメントの場所に `position: absolute;` を書く(書いてある top と left が効きはじめます)
2. つづけて `.card` に `z-index: 2;` を書く
3. `.banner` のコメントの場所に `z-index: 1;` を書く

プレビューで、白いカードがオレンジのバナーの上にのっていたら成功です。

書けたら「できた!」ボタンで判定します。
