# やってみよう

レシピカード6枚を3列のグリッドにならべます。`style.css` を編集しましょう。

1. `.cards` のコメントの場所に `grid-template-columns: repeat(3, minmax(100px, 1fr));` を書く

`.cards` の幅は 720px に固定してあり、列のすきまは 24px です。判定では、各列がちょうど (720 - 24 × 2) ÷ 3 = 224px になっているかを確かめます。

プレビューで、カードが3列 × 2段にならんだら成功です。

書けたら「できた!」ボタンで判定します。
