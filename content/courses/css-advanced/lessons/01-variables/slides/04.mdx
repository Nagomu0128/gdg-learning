# やってみよう

ブログのテーマカラーを、変数で1か所にまとめます。`style.css` を編集しましょう。

`h1` と `.button` には、すでに `var(--main-color)` が書いてあります。ただし肝心の変数がまだ定義されていないので、色が付いていません。

1. ファイルの先頭のコメントの下に `:root { }` のルールを書く
2. その中に `--main-color: #2563eb;` と書いて変数を定義する

書いたとたん、見出しの文字とボタンの背景に同じ青が付くのをプレビューで確かめましょう。

書けたら「できた!」ボタンで判定します。
