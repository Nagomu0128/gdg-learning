# やってみよう

`message.txt` に、コミットメッセージを 1 行書きます。

- `<type>: <日本語の要約>` の形にしましょう
- type は `feat` / `fix` / `docs` などの英語から選びます
- 要約は、何をしたかが伝わるように日本語で簡潔に

たとえば、ログイン画面に入力チェックを足したときは、こう書けます。

```
feat: ログインフォームに入力チェックを追加
```

<Callout type="info">
要約の中身は自由です。type の付け方と、日本語の要約があれば合格です。
</Callout>
