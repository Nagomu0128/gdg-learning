# templateタグ

`<template>` は、あとで使う HTML の「下書き」を入れておくタグです。中に書いたものは画面に表示されません。

```html
<template id="item-template">
  <li></li>
</template>
```

- ブラウザは中身を読み込みますが、表示はしません
- 「同じ形のタグをいくつも作りたい」ときの原本として使います

<Callout type="info">
  リストの項目・カード・表の行など、くり返し出てくる部品の型紙(かたがみ)だと考えると分かりやすいです。
</Callout>
