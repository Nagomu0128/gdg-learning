# 表のアクセシビリティ

このレッスンで学ぶこと

- `<caption>` で表にタイトルを付ける
- `<th>` の `scope` 属性で「どの向きの見出しか」を伝える
- `<tfoot>` で合計などの「まとめの行」を分ける

見た目は同じでも、スクリーンリーダー(画面を音声で読み上げるソフト)にとって分かりやすい表には、ちゃんとした「作法」があります。HTML入門で学んだ表を、だれにでも伝わる表にレベルアップさせましょう。
