# 画像の出し分け

このレッスンで学ぶこと

- `<picture>` タグで「条件によって画像を切り替える」仕組みを作る
- `<source>` の `media` 属性で「どんなときにどの画像を使うか」を指定する
- `<img>` を「ふだん使う画像 + 説明(alt)」として最後に置く

スマホの小さな画面とパソコンの広い画面では、ちょうどいい画像の形がちがいます。広い画面には横長の写真、せまい画面には正方形の写真、のように**出し分け**できるのが `<picture>` です。
