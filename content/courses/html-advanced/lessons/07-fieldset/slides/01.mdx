# フォームをグループ化

このレッスンで学ぶこと

- `<fieldset>` で関連する入力欄をグループにまとめる
- `<legend>` でグループに見出しを付ける
- `autocomplete` 属性でブラウザの自動入力を助ける
- `inputmode` 属性でスマホのキーボードを切り替える

入力欄が多いフォームは、ただ並べるだけだと迷子になります。「お届け先」「お支払い」のようにグループ分けして、入力を助ける属性も付けてあげましょう。
