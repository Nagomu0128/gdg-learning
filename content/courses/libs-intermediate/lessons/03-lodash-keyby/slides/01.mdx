# lodashでデータ変換

このレッスンで学ぶこと

- `_.keyBy` で配列を「キーで引けるオブジェクト」に変換する
- `_.mapValues` でオブジェクトの各値をまとめて変換する
- 一覧データを、目的に合った形に作り替える

<Callout type="info">
配列は「全部を順に見る」のは得意ですが、「id が a1 の商品だけ欲しい」ときは毎回さがす必要があります。キーで引ける形に変えると一発です。
</Callout>
