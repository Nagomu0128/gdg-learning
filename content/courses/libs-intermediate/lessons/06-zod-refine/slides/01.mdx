# zodのカスタム検証

このレッスンで学ぶこと

- `.refine` で、型だけでは表せない「独自ルール」を足す
- 「正の数だけ」「1 文字以上だけ」のような条件を検証する
- `safeParse` の結果で合格・不合格を分ける

<Callout type="info">
`z.number()` は「数かどうか」までしか見ません。`-5` も立派な数です。「正の数だけ」にしたいときは、ルールを自分で足します。
</Callout>
