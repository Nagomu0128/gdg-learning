# zodのネストしたスキーマ

このレッスンで学ぶこと

- `z.object` の中に、さらに `z.object` や `z.array` を入れる
- 入れ子になったデータの「形」をそのままスキーマで表す
- 検証を通れば、深い値も安心して使える

<Callout type="info">
実際のデータは「注文の中に顧客と商品リスト」のように入れ子になっています。スキーマも同じ形に入れ子にすれば検証できます。
</Callout>
