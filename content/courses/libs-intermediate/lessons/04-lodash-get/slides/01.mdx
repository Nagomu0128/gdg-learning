# 深い階層を安全に扱う

このレッスンで学ぶこと

- `_.get` で、深く入れ子になった値を安全に取り出す
- 値が無いときの「既定値」を指定する
- `_.cloneDeep` で、元を壊さない複製を作る

<Callout type="info">
`data.user.profile.address.city` のような深いアクセスは、途中が `undefined` だとエラーになります。lodash なら安全に取り出せます。
</Callout>
