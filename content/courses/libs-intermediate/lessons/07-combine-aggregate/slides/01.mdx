# 複数ライブラリで集計

このレッスンで学ぶこと

- dayjs と lodash を 1 つの処理で組み合わせる
- `_.groupBy` でデータを「グループ分け」する
- `_.mapValues` と `_.sumBy` で各グループを合計する

<Callout type="info">
「売上を月見出しつきでカテゴリ別に集計する」——日付整形は dayjs、集計は lodash、と道具を使い分けます。
</Callout>
