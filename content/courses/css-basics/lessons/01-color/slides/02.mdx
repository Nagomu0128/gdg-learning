# CSSとは

HTMLがページの「骨組み」だとすると、CSSは「見た目」を決めることばです。
文字の色や大きさ、余白、ならべ方などを自由に変えられます。

CSSは `style.css` のようなファイルに書き、HTMLの `head` の中から読み込んで使います。

```html
<link rel="stylesheet" href="style.css" />
```

<Callout type="info">今回の演習では、この読み込みの1行はすでに書いてあります。style.css の中身だけに集中しましょう。</Callout>
