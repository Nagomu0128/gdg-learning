# 大きさを決める width と height

箱の大きさは `width`(横はば)と `height`(高さ)で指定します。

```css
.card {
  width: 300px;
  height: 120px;
}
```

指定しない場合は自動で決まります。

- 横はば … 入る場所いっぱいに広がる
- 高さ … 中身の量に合わせてのびちぢみする

大きさを固定すると、名刺やカードのような「決まった形」を作れます。
