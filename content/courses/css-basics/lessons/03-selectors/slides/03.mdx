# idセレクタ

`id` 属性がついた要素には、`#` で始まるセレクタでスタイルをつけられます。

```html
<h1 id="title">今月のおすすめ図書</h1>
```

```css
#title {
  color: blue;
}
```

`#title` は「id が title の要素」という意味です。
`id` の値はページの中で1回しか使えないので、`#` のセレクタは必ず1つの要素だけに効きます。
