# やってみよう

旅行プランの3つのカードを、横一列にならべます。`style.css` を編集しましょう。

1. `.container` に `display: flex;` を指定して、カードを横にならべる
2. つづけて `justify-content: space-between;` を指定して、両はしまで広げる

プレビューでカードのならびが変わるのを確かめながら進めましょう。

書けたら「できた!」ボタンで判定します。
