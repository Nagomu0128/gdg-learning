# やってみよう

あいさつのカードを、たてならびの中央ぞろえに仕上げます。`style.css` の `.container` にはすでに `display: flex;` が書いてあります。

1. `flex-direction: column;` を足して、たてならびにする
2. `align-items: center;` を足して、横方向の中央にそろえる
3. `row-gap: 16px;` を足して、項目のあいだを空ける

書けたら「できた!」ボタンで判定しましょう。
