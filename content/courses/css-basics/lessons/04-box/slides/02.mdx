# 要素は箱でできている

見出しも段落も、HTMLの要素はすべて四角い「箱」として表示されます。
この箱のしくみを**ボックスモデル**と呼びます。箱は内側から順にこうなっています。

- 中身 … テキストや画像
- `padding` … 中身と線のあいだの余白(内側の余白)
- `border` … 箱のふちの線
- `margin` … 箱の外側の余白

今回はこのうち `padding` と `border` を使って、文章を「カード」らしく見せます。
