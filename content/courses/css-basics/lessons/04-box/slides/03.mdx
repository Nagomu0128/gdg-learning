# 内側の余白 padding

`padding` は上下左右を1方向ずつ指定できます。方向はプロパティ名で表します。

```css
.box {
  padding-top: 16px;
  padding-left: 16px;
}
```

- `padding-top` … 上の余白
- `padding-bottom` … 下の余白
- `padding-left` … 左の余白
- `padding-right` … 右の余白

余白があると、文字が箱のふちにくっつかず、ぐっと読みやすくなります。
