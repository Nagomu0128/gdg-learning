# やってみよう

おしらせの箱を読みやすく整えます。`.box` のルールに書き足しましょう。

1. 上と左に `16px` の内側の余白(`padding-top` と `padding-left`)をつける
2. 下に `2px` の実線(`border-bottom-style` と `border-bottom-width`)をつける

全部で4行です。1行書くごとにプレビューの変化を確かめてみましょう。
