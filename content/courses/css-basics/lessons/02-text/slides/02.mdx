# 文字の大きさ font-size

文字の大きさは `font-size` プロパティで変えられます。
大きさには `px`(ピクセル)という単位をよく使います。数字が大きいほど文字も大きくなります。

```css
h1 {
  font-size: 40px;
}
```

これで `h1` の文字の大きさが 40px になります。

<Callout type="info">数字と px はくっつけて書きます。「40 px」のように空白を入れるとうまく効きません。</Callout>
