# ふだんの色と、のせたときの色

ふだんのスタイルと `:hover` のスタイルは、別々のルールとして書きます。

```css
a {
  color: green;
}

a:hover {
  color: orange;
}
```

- 1つめのルール: ふだんのリンクは green
- 2つめのルール: マウスがのっているあいだだけ orange

<Callout type="warn">`a` と `:hover` のあいだにスペースを入れないでください。`a :hover` と書くと別の意味のセレクタになってしまいます。</Callout>
