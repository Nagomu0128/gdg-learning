# やってみよう

リンク集のリンクに、マウスをのせたときの変化を付けます。`style.css` を編集しましょう。

1. `a` のルールに `color: green;` を書いて、ふだんの色を決める
2. 新しく `a:hover` のルールを作り、中に `color: orange;` を書く

書けたら、プレビューのリンクに実際にマウスをのせて、色が変わるのを確かめてみましょう。

確認できたら「できた!」ボタンで判定します。
