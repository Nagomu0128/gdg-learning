# データと画面を分ける

表示したい中身を、まず配列としてまとめます。

```js
const items = ["おにぎり", "みそ汁", "たまご焼き", "お茶"];
```

この配列が「データ」です。画面(HTML)には空の `<ul>` だけを置いておき、中身は JavaScript が配列から作ります。

<Callout type="info">データと画面を分けておくと、メニューを増やしたいときに配列へ 1 行足すだけで済みます。画面を作るコードは 1 文字も変わりません。実際の Web アプリでも使われている大事な考え方です。</Callout>
