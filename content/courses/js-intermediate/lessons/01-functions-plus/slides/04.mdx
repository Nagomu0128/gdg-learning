# やってみよう

演習では、n を 2 倍にして返すアロー関数をつくり、変数 `twice` に入れます。

```js
const twice = (n) => n * 2;
```

初期コードにある関数 `double` は、この `twice` を呼び出して 2 倍を計算します。`twice` が正しく書ければ、`double(5)` が `10` を返すようになります。

<Callout type="info">コンソールタブに 10 と表示されたら成功です。</Callout>

書けたら「できた!」ボタンで提出しましょう。
