# やってみよう

おみくじを完成させましょう。

1. index.html に `id="draw"` の button と、`id="result"` の表示欄を書く
2. script.js で結果の候補を配列に入れる(一覧から2つ以上)
3. click イベントの中で、ランダムに選んだ結果を `#result` に表示する
4. style.css で、自分らしい見た目に仕上げる

できたら何回か引いてみて、いろいろな結果が出ることをプレビューで確かめてから「できた!」ボタンで判定しましょう。
