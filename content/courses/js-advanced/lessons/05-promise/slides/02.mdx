# 時間のかかる処理

データの読み込みやタイマーなど、結果がすぐには手に入らない処理があります。

```js
console.log("開始");

setTimeout(() => {
  console.log("3秒たちました");
}, 3000);

console.log("終了");
```

- setTimeout は「指定したミリ秒後に、渡した関数を実行して」と予約する命令です
- JavaScript は待っている間も先へ進むので、表示は「開始」「終了」「3秒たちました」の順になります
- このような「あとで結果が届く」処理を非同期処理と呼びます
