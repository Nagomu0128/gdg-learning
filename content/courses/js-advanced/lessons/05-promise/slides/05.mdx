# やってみよう

演習では、300ミリ秒後に "done" で解決する Promise を返す関数 delayedValue を完成させます。

`script.js` には、骨組みと、結果を then で表示する動作確認用のコードがすでに書いてあります。

1. delayedValue の中で、new Promise を使って Promise を作り、return します
2. Promise の中では、setTimeout で300ミリ秒後に resolve("done") を呼びます
3. 「実行」で少し待って done と表示されたら、「できた!」で提出しましょう
