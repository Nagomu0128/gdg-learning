# 総合: TODO リスト

いよいよ JavaScript 上級の総仕上げです。これまで学んだ道具を全部使って、TODO リストをゼロから作ります。

- 配列の状態と `render()` で描き直す型(前レッスン)
- イベントデリゲーション(前々レッスン)
- `createElement` / `classList` / 配列のメソッド

機能は「追加」「完了の切り替え」「削除」「残数の表示」の4つです。
