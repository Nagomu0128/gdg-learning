# やってみよう

演習では、2つの擬似データを取得して合計する async 関数 fetchTotal を完成させます。

`script.js` には、データ取得役の fetchData(指定ミリ秒後に値が届きます)と動作確認用のコードがすでに書いてあります。

1. fetchData(100, 200) の結果を await で受け取り、変数に入れます
2. fetchData(200, 200) の結果も await で受け取り、変数に入れます
3. 2つをたした合計を return します
4. 「実行」で少し待って 300 と表示されたら、「できた!」で提出しましょう
