# やってみよう

演習では、カウンターを作る関数 makeCounter を完成させます。

`script.js` には、骨組みと動作確認用のコードがすでに書いてあります。

1. makeCounter の中で、「呼ばれるたびに count を1増やして、その値を返す関数」を return します
2. 「実行」でコンソールに `[1,2,3]` と表示されたら、「できた!」で提出しましょう
