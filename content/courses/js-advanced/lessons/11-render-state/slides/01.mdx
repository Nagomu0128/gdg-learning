# 状態から描画する

このレッスンで学ぶこと

- 画面の元になるデータを1つのオブジェクト(state)にまとめる考え方
- state を見て画面全体を作り直す `render()` 関数
- 「state を変えたら render() を呼ぶ」という更新の型

ボタンやデータが増えても混乱しないための、UI プログラミングの基本形です。React などのフレームワークも、この考え方の上にできています。
