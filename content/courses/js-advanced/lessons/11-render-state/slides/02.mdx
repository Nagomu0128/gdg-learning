# データと画面を分ける

これまでは、イベントのたびに textContent を直接書き換えてきました。書き換える場所が増えてくると、「今どういう状態なのか」がコードのあちこちに散らばってしまいます。

そこで、画面の元になるデータを1つのオブジェクトにまとめます。

```js
const state = {
  following: false, // フォロー中かどうか
  followers: 100, // フォロワーの数
};
```

本体はあくまで state で、画面は「state の写し」にすぎない、という役割分担で考えます。
