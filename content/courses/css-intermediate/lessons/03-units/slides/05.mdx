# やってみよう

コラムのカードを、単位を使い分けて仕上げます。`style.css` を編集しましょう。

1. `.lead` のルールに `font-size: 1.5rem;` を書く(16px × 1.5 = 24px)
2. `.card` のルールに `width: 50%;` を書く(親の 800px の半分 = 400px)

プレビューで、リード文が大きくなり、カードの幅が半分になって中央にそろうのを確かめましょう。

書けたら「できた!」ボタンで判定します。
