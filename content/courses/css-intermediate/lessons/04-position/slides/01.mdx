# position で要素を配置する

このレッスンで学ぶこと

- 要素を「ふだんの流れ」から外して配置する position
- 基準をつくる `position: relative`
- 基準に重ねる `position: absolute` と `top` / `left`

これまで要素は、HTML に書いた順に上から下へならんでいました。position を使うと、この流れから要素を取り出して、写真の上のバッジのように好きな場所へ重ねられます。
