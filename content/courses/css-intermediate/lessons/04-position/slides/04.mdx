# やってみよう

新商品の写真に「NEW」バッジを重ねます。`style.css` を編集しましょう。

1. `.photo` のルールに `position: relative;` を書いて、基準にする
2. `.badge` のルールに `position: absolute;` を書いて、流れから浮かせる
3. つづけて `top: 12px;` と `left: 12px;` を書いて、写真の左上に置く

プレビューで、バッジが写真の左上に重なるのを確かめましょう。

書けたら「できた!」ボタンで判定します。
