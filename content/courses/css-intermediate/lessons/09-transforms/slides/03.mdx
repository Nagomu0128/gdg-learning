# scale() で拡大・縮小する

大きさを変えるのは `scale()` です。値は倍率で、1 が等倍です。

```css
h1 {
  transform: scale(1.2);
}
```

- `scale(1.2)` は1.2倍に拡大、`scale(0.5)` は半分に縮小

transform のおもしろいところは、**まわりのレイアウトを動かさない**ことです。font-size を大きくすると後ろの要素が押し出されますが、scale() は見た目だけが変わり、ほかの要素はその場にとどまります。
