# rotate() でかたむける

`transform` は、要素の形や向きを変えるプロパティです。値には「どう変形するか」を関数の形で書きます。要素をかたむけるのは `rotate()` です。

```css
.badge {
  transform: rotate(-10deg);
}
```

- 角度は `deg`(度)の単位で書きます
- プラスの値は時計回り、マイナスの値は反時計回りです

<Callout type="info">rotate(45deg) なら時計回りに45度、rotate(-10deg) なら反時計回りに10度かたむきます。</Callout>
