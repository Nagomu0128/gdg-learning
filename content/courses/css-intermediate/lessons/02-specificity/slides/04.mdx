# やってみよう

図書室のお知らせページを仕上げます。段落は全部 `p` ですが、1つだけ `class="note"` が付いた重要なお知らせがあります。

1. `style.css` にある `p { color: dimgray; }` は**そのまま残す**
2. `.note` のルールを新しく追加して、`color` を `crimson` にする

クラスセレクタは要素セレクタより詳細度が高いので、`class="note"` の段落だけが赤くなり、ほかの段落は dimgray のまま — これが確認できたら成功です。

書けたら「できた!」ボタンで判定します。
