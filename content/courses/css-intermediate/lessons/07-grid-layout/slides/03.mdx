# ます目を区切る「グリッド線」

Grid では、ます目を区切る線に**番号**が付いています。2列のグリッドなら、たての線は「左はし・まんなか・右はし」の3本で、左から順に 1・2・3 です。

```css
.page {
  display: grid;
  grid-template-columns: 200px 400px;
}
```

- 1列目 = 1番の線から2番の線まで
- 2列目 = 2番の線から3番の線まで
- 列が2つなら、線は列より1本多い3本になります

この線の番号を使うと、要素に「どこからどこまで広がるか」を指示できます。次のスライドで使ってみましょう。
