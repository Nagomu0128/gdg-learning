# Flexbox 応用

このレッスンで学ぶこと

- あまった幅を分け合う `flex-grow`
- 幅の基準を決める `flex-basis`
- 折り返しの `flex-wrap` と、ならび順の `order`

`display: flex;` で横にならべる方法は CSS 入門で学びました。今回は「どの子がどれだけ広がるか」「入りきらないときにどうするか」まで、ならべ方を細かく指示できるようになります。
