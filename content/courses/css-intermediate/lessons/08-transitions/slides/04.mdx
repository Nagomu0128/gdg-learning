# やってみよう

「参加を申し込む」ボタンの色が、マウスをのせるとなめらかに変わるようにします。`style.css` を編集しましょう。

1. `a` のルールに `transition-property: background-color;` を書く
2. つづけて `transition-duration: 0.3s;` を書く
3. `a:hover` のルールを新しく書いて、`background-color: #0369a1;` を指定する

プレビューでボタンにマウスをのせて、色がふわっと変わるのを確かめましょう。

書けたら「できた!」ボタンで判定します。
