// 学習イベント計測(DesignDoc §10.3)。STUB(F が実装): env.ANALYTICS?.writeDataPoint に置き換える。
import type { Env } from "~/lib/env";

export function track(_env: Env, _event: string, _data?: Record<string, unknown>): void {
  // no-op
}
