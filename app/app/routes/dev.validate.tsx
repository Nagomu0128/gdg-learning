// STUB(B が実装): 自己整合性検証ページ(DEV_LOGIN=1 時のみ)。CONTRACTS §8。
export default function DevValidate() {
  return (
    <main className="mx-auto max-w-3xl p-8">
      <h1 className="font-bold text-2xl text-slate-900">教材 自己整合性検証</h1>
      <p className="mt-4 text-slate-500 text-sm" data-testid="validate-summary">
        TODO: 検証ページ(担当: B)
      </p>
    </main>
  );
}
