// STUB(C が実装): 開発用ログイン。env.DEV_LOGIN === "1" のときだけ有効。CONTRACTS §4.3。
export function loader() {
  return new Response("Not Found", { status: 404 });
}

export function action() {
  return new Response("Not Found", { status: 404 });
}
